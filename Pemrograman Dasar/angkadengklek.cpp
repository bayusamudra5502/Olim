#include <bits/stdc++.h>
using namespace std;

int main(){
    int hasil = 0, tmp;

    while(scanf("%d", &tmp) != EOF){
        hasil += tmp;
    }

    printf("%d\n", hasil);
    return 0;
}